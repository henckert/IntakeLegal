# Server Environment Variables
# Copy this to .env and fill in real values

# Required for AI processing (entity extraction)
OPENAI_API_KEY=

# Required for database (Neon PostgreSQL pooled connection)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Optional: Direct connection for migrations (non-pooled)
DIRECT_DATABASE_URL=

# Optional: Email notifications via Resend
RESEND_API_KEY=

# Optional: Clerk authentication (for protected firm routes)
CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# App URLs
APP_BASE_URL=http://localhost:3000
SERVER_BASE_URL=http://localhost:4000
NODE_ENV=development
